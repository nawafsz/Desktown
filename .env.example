# --- SERVER CONFIGURATION ---
NODE_ENV=production
PORT=5000
STANDALONE_PORT=5001

# --- DATABASE CONNECTION ---
# Supabase / Neon / Local Postgres
# Example: postgresql://postgres:password@db.project-id.supabase.co:5432/postgres
DATABASE_URL=your_postgresql_connection_string

# --- AUTHENTICATION ---
# A secure secret for session encryption
SESSION_SECRET=your_random_secret_string

# --- GOOGLE CLOUD STORAGE (Standalone Mode) ---
# Options for GCS integration without Replit sidecar
# Option 1: Using a path to a JSON key file
GOOGLE_APPLICATION_CREDENTIALS=./gcs-key.json

# Option 2: Using environment variables for credentials
GCS_PROJECT_ID=your_project_id
GCS_CLIENT_EMAIL=your_service_account_email
GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Key-Here\n-----END PRIVATE KEY-----\n"

# Search paths for public objects (comma separated)
PUBLIC_OBJECT_SEARCH_PATHS=public-bucket/assets
PRIVATE_OBJECT_DIR=private-bucket/uploads

# --- STRIPE PAYMENTS (Standalone Mode) ---
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# --- AI INTEGRATION (Optional) ---
GOOGLE_API_KEY=your_gemini_api_key

# --- REPLIT COMPATIBILITY (Optional) ---
# Set these if you want to mock Replit environment
REPL_ID=standalone-production
REPL_SLUG=desktown
